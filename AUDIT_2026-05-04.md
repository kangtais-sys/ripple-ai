# Ssobi 풀 시스템 Audit — 2026-05-04

> "메타 승인만 받으면 바로 유저가 가입해서 이용할 수 있을 정도야?"
> **답: 아직 아니다.** 5개 차단 이슈 + 3개 작은 이슈 발견. 모두 1-2시간 안에 해결 가능.

## 🚨 차단 이슈 (Meta 검수 영상 찍기 전 반드시 해결)

### B1. 미배포 — production 에 토글 wire-up 없음
- ssobi.ai/app 에서 `ar-master-toggle`, `ar-ch-comment`, `app-language-select` 모두 **존재하지 않음**
- 오늘 만든 코드가 로컬에만 있고 Vercel 에 안 올라갔어
- **해결**: `git add . && git commit && git push` (또는 Vercel CLI deploy)

### B2. Migration 012 미적용 — 온보딩 DB 깨짐
- `profiles.onboarding_topics`, `onboarding_tone`, `onboarding_goal`, `preferred_template` 등 컬럼 없음
- **결과**: 신규 가입자가 온보딩 마치면 `/api/user/onboarding` 이 500 에러
- **해결**: 아래 SQL 한 번 실행

### B3. Migration 013 미적용 — 개인화 추천 DB 깨짐
- `user_daily_recs` 테이블 자체 없음
- **결과**: cron `refresh-recommendations` 실행 시 500 에러, 홈에 개인화 추천 안 뜸
- **해결**: 아래 SQL

### B4. Public 링크 페이지 — 발행된 게 0개
- `/u/yuminhye` → 404 (테이블에 published 행 없음)
- 내 링크 탭은 자동 동기화(2초 디바운스) 가 있지만 **실 사용자가 한 번도 발행 안 함**
- **결과**: Meta 영상에서 "내 링크 페이지" 시연 불가
- **해결**: 본인 로그인 → 내 링크 탭 → 한 번이라도 편집 → 자동 publish 됨

### B5. card_news_jobs 데이터 비어있음 (4월 28일 시도 실패)
- DB 에 3개 행 있지만 모두 `hook=null, body=[], scores=null` — 생성 중 크레딧 부족으로 실패
- **결과**: 카드뉴스 시연 불가 (이전 데이터 못 씀)
- **해결**: 크레딧 충전됐으니 새로 1개 만들면 됨

---

## ⚠️ 작은 이슈 (영상 후 베타 운영 전까지)

### S1. ESLint 1 error
- `src/app/api/cron/trend-research/route.ts:149:7` — `let attempt` → `const attempt` 권장. 1초 fix.

### S2. `scheduled_posts` / `points_history` / `invitations` 테이블 없음
- 코드 어디서도 안 씀 (app.html 의 mock UI 만 있음)
- 베타 정식 출시 전 결정: 정말 만들 건지 / mock 으로 둘 건지

### S3. 캐시된 dev-mode
- `body.dev-mode` 가 production 에서도 켜져 있음 (네 브라우저 localStorage 때문)
- 일반 사용자에겐 안 보이지만, 너 화면에선 dev 시뮬 버튼들 계속 보임
- **해결**: `?dev=0` 한 번 방문하면 끝

---

## ✅ 검증 완료 (작동 확인)

```
✅ Anthropic API           : 응답 정상 (haiku/sonnet)
✅ Trend cron              : 2026-05-04 top5+recommended 생성
✅ Tone profile            : sisru_doku 학습 완료, generateReply 활용 검증
✅ Webhook 게이트          : global/channel/thread takeover 모두 정상
✅ 토글 onchange + 동기화   : 4개 핸들러 + 페이지 로드 sync (로컬)
✅ Migration 014 적용       : profiles 컬럼 3개 + dm_threads + RLS
✅ TS 타입 체크            : pass
✅ DB 코어 테이블          : profiles/ig_accounts/tone_profiles/usage_logs/reply_logs/
                             daily_trends/dm_threads/card_news_jobs/short_links 등 정상
✅ ig_accounts 연결        : sisru_doku (token expires 2026-06-19)
✅ link_pages 라우트        : /u/[handle]/page.tsx 정상 (DB 비어있을 뿐)
```

---

## 📋 Catch-up SQL (한 번 실행)

Supabase Dashboard → SQL Editor → 아래 통째로 붙여넣기 → Run

```sql
-- Migration 012: 온보딩 컬럼
ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS onboarding_topics TEXT[] DEFAULT '{}',
  ADD COLUMN IF NOT EXISTS onboarding_tone TEXT,
  ADD COLUMN IF NOT EXISTS onboarding_goal TEXT,
  ADD COLUMN IF NOT EXISTS preferred_hook_type TEXT,
  ADD COLUMN IF NOT EXISTS preferred_template TEXT,
  ADD COLUMN IF NOT EXISTS generated_count INT DEFAULT 0,
  ADD COLUMN IF NOT EXISTS category_history JSONB DEFAULT '{}'::jsonb,
  ADD COLUMN IF NOT EXISTS onboarding_completed_at TIMESTAMPTZ;

CREATE INDEX IF NOT EXISTS profiles_onboarding_topics_idx
  ON public.profiles USING gin (onboarding_topics);

-- Migration 013: 개인화 추천 테이블
CREATE TABLE IF NOT EXISTS public.user_daily_recs (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  date_kst    DATE NOT NULL,
  generated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  topics      JSONB NOT NULL DEFAULT '[]'::jsonb,
  meta        JSONB NOT NULL DEFAULT '{}'::jsonb,
  UNIQUE (user_id, date_kst)
);

CREATE INDEX IF NOT EXISTS user_daily_recs_user_idx
  ON public.user_daily_recs (user_id, date_kst DESC);

ALTER TABLE public.user_daily_recs ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "user_daily_recs read own" ON public.user_daily_recs;
CREATE POLICY "user_daily_recs read own" ON public.user_daily_recs
  FOR SELECT USING (auth.uid() = user_id);
```

---

## 🎯 우선순위 액션 (이 순서대로)

| # | 누가 | 무엇 | 시간 |
|---|---|---|---|
| 1 | **너** | 위 Catch-up SQL Supabase 실행 | 1분 |
| 2 | **나** | ESLint 에러 1건 fix | 1분 |
| 3 | **너 → "배포해줘"** | git commit + push (production 에 토글 올리기) | 즉시 |
| 4 | **너** | 본인 로그인 → 내 링크 탭 한 번 편집 (자동 publish 됨) | 2분 |
| 5 | **너** | 만들기 탭 → 주제 골라 카드뉴스 1개 생성 (실 데이터 시연용) | 3분 |
| 6 | **너** | sisru_doku 게시물에 다른 IG 로 댓글 달아 자동 응대 실증 | 1분 |
| 7 | **나** | Phase 4 (Meta 영상 시나리오 + 영문 사용 사례) 작성 | 1-2h |
| 8 | **너** | OBS 영상 6개 녹화 + Meta 폼 제출 | 2-3h |

---

## 🎬 그제서야 "메타 승인만 받으면 바로 운영 가능" 인 상태가 됨

지금 시점에서 신규 사용자가 가입하면 온보딩에서 막힘. **위 1·3 만 해결되면 신규 가입 → 운영 가능 상태**. 4·5·6 은 영상 찍기 위해서 한 번씩 해두는 검증 단계.
