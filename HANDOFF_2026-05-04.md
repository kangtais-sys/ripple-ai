# Ssobi 핸드오프 — 2026-05-04 (Phase 1.1 검증 완료 후)

## 한 줄 컨텍스트
Meta App Review 재신청 + 운영 가능 수준까지 끌어올리는 중. **Phase 1.1 토글 wire-up + DB 검증까지 완료**. 다음은 Phase 1.4-1.5, Phase 2 (AI 기능 실증), Phase 4 (영상 촬영 + 재신청).

## 검증 완료 (다시 안 해도 됨)
```
✅ Anthropic API           : 응답 정상 (haiku/sonnet)
✅ Trend cron              : 2026-05-04 top5 + recommended 정상 생성
✅ tone_profile            : sisru_doku 학습 완료 (친근체+이모지+banned_words)
✅ Migration 014           : profiles 컬럼 3개 + dm_threads + RLS 적용됨
✅ Auto-reply 토글 wire-up :
   ├─ 글로벌 토글       (auto_reply_enabled)
   ├─ 채널별 토글 (댓글) (auto_reply_channels.ig_comment)
   ├─ 채널별 토글 (DM)   (auto_reply_channels.ig_dm)
   └─ 언어 스위처 (ko/en) (app_language)
✅ Webhook 게이트 로직     :
   ├─ 글로벌 OFF → reply_logs 에 skip_reason='global_toggle_off'
   ├─ 채널 OFF  → skip_reason='channel_toggle_off'
   ├─ thread takeover → skip_reason='thread_takeover'
   └─ ON       → AI 답변 생성 (학습 말투 그대로 작동 확인)
✅ TS 타입 체크            : tsc --noEmit pass
✅ DEV-only 시뮬 버튼      : .dev-only 가드 정상 (?dev=1 일 때만 노출)
```

## 변경된 파일 (이미 적용됨, 재작업 X)
```
src/app/api/auto-reply/toggle/route.ts        신규 (GET/POST)
src/app/api/dm-threads/takeover/route.ts      신규 (GET/POST)
src/app/api/profile/language/route.ts         신규 (GET/POST + Accept-Language)
src/app/api/webhook/instagram/route.ts        토글 게이트 + 스킵 로깅 추가
public/app.html                                토글 UI + 채널별 + 언어 스위처 + JS 핸들러
supabase/migrations/014_auto_reply_controls.sql  신규 (적용 완료)
```

---

## 🚨 사용자(유민혜)만 가능한 것 (Claude 못 함)

### 1. millimilli.official 도 IG OAuth 연결
- **현재 상태**: sisru_doku 만 ig_accounts 에 등록됨
- **방법**: 본인 로그인 → 내정보 → 계정 연동 → Instagram → millimilli 로 로그인
- **왜 필요**: Meta 영상에서 두 계정 운영 시연 (다중 계정 = 프로 플랜 가치 증명)

### 2. (선택) UI 토글 시각 확인
- 본인 로그인 → 키우기 → AI 응대 카드 펼치기 → 토글 ON/OFF 한 번씩 눌러보기
- 내정보 → 언어 영어로 바꿔보기 (저장만 됨)

### 3. Meta 재신청 폼 제출 (영상 완성 후)
- 영상 + 영문 사용 사례 + 제출 버튼은 본인 계정으로만 가능

---

## 🤖 새 Claude 세션이 할 일 (우선순위 순)

### A. Phase 1.4 — 응대 룰 audit (1h)
**파일**: `public/app.html` 의 `m-dm-rule` 모달
- 댓글 → DM 자동 발송 룰 설정 모달이 실제로 작동하는지 검증
- 룰 저장 → DB 어디에 저장되는지 추적
- 활성/비활성 토글 작동 확인
- 만약 mock 만 있고 실제 저장 안 되면 → 백엔드 endpoint 만들어서 wire up

### B. Phase 1.5 — 응대 로그 UI 강화 (2h)
**파일**: `public/app.html` 키우기 → 실시간 관리 → 댓글·DM 탭
- 현재 reply_logs 표시는 'sent' 만 위주
- **추가 필요**: send_status='skipped' 항목도 가시화 (Meta 영상 핵심 증거)
  - "자동 응대 OFF — 사용자 통제" 라벨로 표시
  - skip_reason 별 색상 분리 (global_toggle / channel_toggle / thread_takeover)
- 룰 매칭 정보 표시 ("dm_rule:이벤트참여 매칭" 같은)

### C. Phase 2 — AI 기능 실증 (코드 audit 만, 2-3h)
실제 호출은 auth 필요해서 어려움. 코드 audit 으로 issue 찾기:
- `src/app/api/cardnews/generate/route.ts` — 4 템플릿 별 정상 작동? 에러 핸들링?
- `src/app/api/tone/learn/route.ts` — 분석 → tone_profiles 저장 흐름
- `src/app/api/tone/fetch-posts/route.ts` — IG 게시물 자동 수집
- `src/app/api/replies/[id]/approve/route.ts` — 긴급 케이스 승인 발송
- 발견된 이슈 즉시 수정 + 테스트 데이터로 검증

### D. Phase 4 (Phase 3 건너뛰고 직진) — Meta 재신청 자료 (3-4h)
**가장 중요**. 이전 거부 사유 = "스크린캐스트가 사용 사례와 일치하지 않음"

**필요한 결과물**:
1. **영문 사용 사례 (3개 권한 별)**:
   - `instagram_business_basic` — IG 프로필/게시물 read 사용 사례
   - `instagram_manage_comments` — 댓글 자동 응대 사용 사례  
   - `instagram_business_manage_messages` — DM 자동 응대 사용 사례
   - 각 200-300단어, "Ssobi differentiates as 'reply like myself' SaaS for K-MZ influencers" 톤
   
2. **영상 시나리오 6개 (각 30-60초)**:
   ```
   1) 가입 → 온보딩 (앱 가치 제안)
   2) IG OAuth 연결 (sisru_doku 또는 millimilli)
   3) 말투 학습 (게시물 자동 수집 + Claude 분석)
   4) 댓글 자동 응대 (실제 IG 댓글 또는 dev 시뮬 — 5초간 토글 ON 비추기)
   5) DM 자동 응대 (긴급 케이스 → 사람 검토 + 승인 발송)
   6) 사용자 통제권 (글로벌 토글 OFF → reply_logs 에 'skipped' 표시)
   ```
   
3. **각 영상 영문 자막 스크립트** (30초 / 60초 버전)
4. **녹화 가이드**: OBS 셋업 / 화면 사이즈 / 마우스 강조

### E. 끝나면 사용자에게 인계
- 영상 시나리오 + 영문 자막 + 사용 사례 → 사용자가 OBS 로 녹화 → Meta App Review 폼 제출

---

## 핵심 컨텍스트 파일 포인터

```
HANDOFF_2026-05-04.md         이 문서 (최신 상태)
CLAUDE.md                      프로젝트 규칙 (legacy/repli_v3.html 디자인 절대 보존)
public/app.html                메인 앱 (14k+ 줄, 토글 라인 ~2113-2148)
src/app/api/webhook/instagram/route.ts   웹훅 (handleComment / handleMessage / generateReply)
src/lib/auth-helper.ts        getUserFromRequest (Bearer + 쿠키 둘 다)
supabase/migrations/014_auto_reply_controls.sql  최신 마이그레이션
```

## Supabase / IG / Anthropic 자격증명
- Supabase project: `ffozahaztbudvsnnkvep` (.env.local 에 SR_KEY)
- IG 연결된 계정: `sisru_doku` (id 6633cb22..., user 9161c37c...)
- Meta 앱: `Repli` (973683215179192) — App Review 거부됨, 재신청 필요
- 사용자 본인 계정: kangtais@naver.com (user_id 9161c37c-9a9d-45cd-ac4d-fbba28664304)

## 핵심 의도 (잊지 말 것)
- **자동 응대는 Ssobi 핵심 가치** — per-message 사람 승인 X. "본인처럼 응대"
- **사용자 통제권은 토글로** — 글로벌·채널별·thread 별 OFF 가능 (이미 구현)
- **긴급 케이스만 사람 검토** — 환불/부정 이슈 등 (기존 approveDraft 흐름)
- **"상담원 연결" 표현 금지** — Ssobi 는 상담원 아니라 본인 대체 서비스

## ⚠️ 검수 통과의 진짜 변수
**토글이 통과의 본질이 아님**. 토글은 "있어야 하는 보강". **진짜 거부 사유는 영상**.
영상에서 권한 3개가 어떻게 쓰이는지 명확히 보여야 함. Phase 4 가 본 게임.
